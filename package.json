{
  "name": "@fuse-labs/fuse-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/client",
    "apps/host",
    "packages/*",
    "plugins/**/*"
  ],
  "scripts": {
    "client:dev": "yarn workspace client dev",
    "host:build": "yarn workspace host build",
    "host:dev": "yarn workspace host dev",
    "host:start": "yarn workspace host start",
    "dev": "yarn workspace client dev & yarn workspace host dev"
  },
  "packageManager": "yarn@3.3.0",
  "devDependencies": {
    "supports-color": "^8",
    "typescript": "^4.9.3"
  }
}
