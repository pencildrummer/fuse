import Badge from "../react-daisyui/Badge/Badge";

export default function PrinterStatusBadge(printer) {
  return (
    <Badge variant="primary" className="text-sm uppercase font-semibold">
      CONNESSA
    </Badge>
  )
}