import MainLayout from "../components/layouts/MainLayout";

export default function DynamicPage(props) {
  return <MainLayout>
    TODO: Dynamic page for registered plugins. Register routes on plugin install?
  </MainLayout>
}